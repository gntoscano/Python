#This program check the price and the name of an item and returns: *Total price, *Number of itens that costs more than 1000, *The cheapest item

sum = count1000 = cont = menor = 0
nomenor = ''

while True:
    name = str(input("Write the name of the product: "))
    price = float(input("Write the pruce of the product: "))

    if price > 1000:
        count1000 += 1

    cont += 1
    sum += price

    if cont == 1 or price < menor:
        menor = price
        nomenor = name

    print("-=" * 20)
    continuar = str(input("Deseja continuar? [S]/[N]: ")).upper().strip()[0]
    if continuar == 'N':
        break


print(f"O total gasto na compra foi de R${sum:.2f}")
print(f"{count1000} produto(s) custa(m) mais de R$1000")
print(f"O produto mais barato foi {nomenor} e custa R${menor:.2f}")

